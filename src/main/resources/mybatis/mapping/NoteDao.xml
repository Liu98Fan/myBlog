<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.bestrivenlf.myweb.interfaceDao.NoteDao">

    <!--<select id = "saveNote" parameterType="cn.bestrivenlf.myweb.entity.Note" >-->
        <!--call saveNote(#{id},#{title},#{author},#{content},#{date},#{newdate},#{del_flag});-->
    <!--</select>-->
    <!--<select id="getNoteCount" resultType="Integer">-->
        <!--select count(*) from note_tb where del_flag = 0;-->
    <!--</select>-->

    <!--<select id="getNotesForPage" resultType="cn.bestrivenlf.myweb.entity.Note" parameterType="Integer">-->
        <!--select * from note_tb WHERE-->
		<!--del_flag = 0-->
		<!--ORDER BY-->
		<!--date DESC-->
		<!--LIMIT #{0},#{1};-->
    <!--</select>-->

    <!--<select id="getNote" parameterType="String" resultType="cn.bestrivenlf.myweb.entity.Note">-->
        <!--select  * from note_tb where del_flag = 0 and id = #{id}-->
    <!--</select>-->
</mapper>